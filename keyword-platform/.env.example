# 复制本文件为 .env，把 DATABASE_URL 换成你自己的连接串。
# .env 已被 .gitignore 忽略，不会提交到 GitHub（里面含密码）。
#
# Supabase：项目 → Settings → Database → Connection string → URI（勾选 "Use connection pooling" 更稳）
#   形如： postgresql://postgres.xxxx:[密码]@aws-0-xx.pooler.supabase.com:6543/postgres
#
# Neon：项目 Dashboard → Connection string（带 ?sslmode=require）
#   形如： postgresql://user:[密码]@ep-xxx.aws.neon.tech/neondb?sslmode=require

DATABASE_URL=postgresql://用户名:密码@主机:端口/数据库名
